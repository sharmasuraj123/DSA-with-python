# arr,hashMap = [],{}
# for i in nums:
#     hashMap[i] = hashMap.get(i,0)+1
# sortedHash = dict(sorted(hashMap.items(),key=lambda x: x[1], reverse=True))
# for idx,val in sortedHash.items():
#     if len(arr)== k:
#         return arr
#     arr.append(idx)
# return arr



